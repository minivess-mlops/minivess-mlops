# UlikeMamba model config group entry.
# U-Like Mamba: U-Net with Mamba SSM blocks (Wang 2025).
# Pure-PyTorch SSM approximation — no mamba-ssm CUDA kernels required.
model: ulike_mamba
model_family: ulike_mamba
architecture_params:
  d_model: 256
  n_layers: 4
  d_state: 16
  expand: 2
  pretrained: false
