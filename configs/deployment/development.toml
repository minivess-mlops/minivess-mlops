[development]
debug = true
# mlflow_tracking_uri removed — read from MLFLOW_TRACKING_URI env var (.env.example)
langfuse_enabled = true
braintrust_enabled = false
agent_provider = "ollama:qwen2.5-coder:14b"
test_markers = "unit and not slow"
