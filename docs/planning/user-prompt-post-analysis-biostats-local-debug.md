# User Prompt (Verbatim) — 2026-03-20

## Context

After completing biostatistics phases 2-6 and the alpha hardcoding fix, the user
requested the following multi-part planning and implementation effort:

## Verbatim Prompt

> After this, then as a last step in this debugging plan, we need to make sure that the post-training Flow is good enough for our debugging run. Let's start planning to /home/petteri/Dropbox/github-personal/minivess-mlops/docs/planning/post-training-flow-debug-double-check.xml and make sure that it knows how to create new mlruns to the same experiment name as in Model Flow (i.e. mlrun=dynunet-params -> mlrun=dynunet-params-multiswa so that we have individual mlruns reflecting the combinations) allowing the easy factorial stats in Biostatistics and easy ensembling in Analysis Flow. The post-training Flow is not very complex and it only involves any technique that can be applied rather model-agnostically on top of trained models (either 3rd party pretrained model like SAM3 that we never touched, or dynUnet trained from scratch). This is opposed to end-to-end training which could for example include joint denoising and segmentation, joint segmentation with graph creation and downstream mesh creation (e.g. https://arxiv.org/html/2412.00840v1) as we don't have differentiable Prefect flows and the gradient do no flow now across Prefect Flows (e.g. something like http://arxiv.org/abs/2602.04430, and "DiffML: End-to-end Differentiable ML Pipelines: "Inthis paper, we present our vision of differentiable ML pipelines called DiffML that truly allows to automate the construction of ML pipelines in an end-to-end fashion. DiffML allows to jointly train not just the ML model itself but also the entire pipeline including data engineering steps, e.g., data cleaning, data augmentation, etc. Our core idea is to formulate all steps in a differentiable way such that the entire pipeline can be trained using backpropagation. However, this is a non-trivial problem and opens up many new research questions. To show the feasibility of this direction, we demonstrate initial ideas and a general principle of how typical data engineering steps can be formulated as differentiable programs and jointly learned with the ML model. Moreover, we discuss a research roadmap and core challenges that have to be systematically tackled to enable fully differentiable ML pipelines."" https://doi.org/10.1145/3595360.3595857 (https://scholar.google.co.uk/scholar?cites=7709396815445588882&as_sdt=2005&sciodt=0,5&hl=en). Read all the files in /home/petteri/Dropbox/github-personal/minivess-mlops/knowledge-graph LINE-BY-LINE to reduce your context amnesia and update the .xml plan with multiple reviewer agent runs.
>
> After you have done this, we need to test the 3 different Flows (Post-training -> Analysis -> Biostatistics) locally getting the DynUnet results from MLflow testing also the decoupling feature of our Prefect Flow as we have the Model Flow as the expensive GPU flow with the following steps possible on normal consumer desktops/laptops so we should be able to extract the artifacts "on-the-fly from Cloud" or there should be a manual download / sync mechanism implemented that would download all the artifacts to a local disk for fully local work (this is OBLIGATORY now). Thus no need for advanced real-time sync mechanism but a manual download is fine at this point (Add a P2 Issue on the advanced sync). Create /home/petteri/Dropbox/github-personal/minivess-mlops/docs/planning/debug-factorial-local-post-analysis-biostats.xml optimized with multiple rounds of iteration serving as a local "integration test" of /home/petteri/Dropbox/github-personal/minivess-mlops/docs/planning/post-training-flow-debug-double-check.xml /home/petteri/Dropbox/github-personal/minivess-mlops/docs/planning/analysis-flow-debug-double-check.xml /home/petteri/Dropbox/github-personal/minivess-mlops/docs/planning/biostatistic-flow-debug-double-check.xml files then. Then create a deterministic .sh plan that is run by that /home/petteri/Dropbox/github-personal/minivess-mlops/docs/planning/debug-factorial-local-post-analysis-biostats.xml which involves the use of LOCAL /home/petteri/Dropbox/github-personal/minivess-mlops/.claude/skills/factorial-monitor Skill for LOCAL run now before involving the actual debug run with all the 4 flows as before on GCP. Does this seem clear? Read again every doc that I have you LINE-by-LINE, no key points, no cheating, nothing. Use maximum effort and ask me up to 20 questions in your interactive format to make sure our visions align! Save my prompt verbatim and let's start and I will go offline for few hours so could you structure your work that you do as much work without asking me first, and then save all the questions to the last so that the questions are there ready waiting for me with you having been able to create all those new .xml plans as much as you can without asking me questions. Starting with some basic understand of how this "Post-training -> Analysis -> Biostats" work, and explore with reviewer agents if our kg is actually optimally expressing the idea of these three Flows and how these connect to Model Flow and how to Biostats Flow

## Deliverables

1. `docs/planning/post-training-flow-debug-double-check.xml` — Post-training flow plan
2. `docs/planning/analysis-flow-debug-double-check.xml` — Analysis flow plan (needs questions)
3. `docs/planning/debug-factorial-local-post-analysis-biostats.xml` — Local integration test plan
4. Deterministic `.sh` script for local factorial run
5. P2 issue for advanced cloud artifact sync
6. Questions saved for user review (up to 20)
