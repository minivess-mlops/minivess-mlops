# Batch Literature Research — Original Prompt (Verbatim)

```
Plan how to use this:

[create-literature-report skill v1.0.0/v2.1.0 description]

For the following new themes that need to be explored, exactly like we did for
/home/petteri/Dropbox/github-personal/minivess-mlops/docs/planning/biomedical-agentic-ai-research-report.md,
and let's create a generic literature research report plan
/home/petteri/Dropbox/github-personal/minivess-mlops/docs/planning/batch-literature-research-report.xml
which will outline how these 4 themes should be "jointly" (well in serial fashion is alright
computationally) as there is some semantic overlap when contextualized to our repo as desired,
but we would want these 4 reports and the 5th existing biomedical-agentic-ai-research-report.md
still provide unique insights and let's not repeat the same themes in these reports. The batch
report should also address how we should be updating our knowledge-graph and OpenSpec SDD planning
to support both the efficient code development in the future as well as our manuscript writing.
Do NOT forget this dual-mandate as we should view the knowledge graph in this repo the ground truth
with the code in this repo and the written .tex manuscripts to be downstream projections then based
on this ground truth (see for example our vision in /home/petteri/Dropbox/LABs/CV/cv-repo). This
can also be seen as the final "scientific double-check" of the completeness of our repo, and what
we are logging before the full factorial experiment (not the minimum research experiment, but the
full one with all the post-training methods, different ensembling methods, etc, to be run with
Skypilot/Pulumi for GCP GPU training).

Let's create a new branch doc/literature-research-reports and start planning the .xml plan with
reviewer agents that could create some simple .sh script without any "advanced screen use" (see
CLAUDE.md) as these never work so very basic script that runs the scripts sequentially with some
exposure to some .log file hopefully! Or if there are any doubts that this .log approach will
freeze. Let's simply plan with this initial .xml plan how to create those 4 child .xml plans for
each literature research theme that I simply run manually to avoid wasting a lot of time waiting
and thinking that something will happen? Either way create these 4 .xml plans and the .sh is just
an optional script that I decide or not decide to use. Save my prompt verbatim and let's start
planning!
```
