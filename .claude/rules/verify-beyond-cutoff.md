---
paths:
  - "src/minivess/adapters/**"
  - "src/minivess/pipeline/**"
---

When implementing with a library near or beyond training knowledge cutoff:
1. ALWAYS web-search to verify the library exists and get correct API
2. If Context Hub is available, run `chub get <library>` for curated docs
3. NEVER confabulate API signatures — say "I'm not sure" and verify
4. SAM3 = Meta's SAM 3 (Nov 2025), NOT SAM2. Always verify.

Rule #10 in CLAUDE.md. See: .claude/metalearning/2026-03-02-sam3-implementation-fuckup.md
