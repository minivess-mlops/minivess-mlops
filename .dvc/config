[core]
    remote = minio
['remote "minio"']
    url = s3://dvc-data
    endpointurl = http://localhost:9000
    access_key_id = minioadmin
    secret_access_key = minioadmin
# remote_storage (s3://minivessdataset) REMOVED — AWS is BANNED. See CLAUDE.md.
# remote_readonly REMOVED — same reason.
['remote "gcs"']
    url = gs://minivess-mlops-dvc-data
