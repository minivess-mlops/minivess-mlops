# MinIVess MLOps — HPO Training on GCP via SkyPilot
#
# Hyperparameter optimization using Optuna + ASHA (HyperbandPruner).
# Uses Docker image with all deps pre-installed.
#
# Launch (MUST use 'sky jobs launch' for spot recovery):
#   sky jobs launch deployment/skypilot/train_hpo.yaml \
#     --env-file .env --env MODEL_FAMILY=dynunet \
#     --env HPO_CONFIG=configs/hpo/dynunet_example.yaml -y
#
# Docker image: All deps pre-installed. Setup only pulls data + copies config.
# BANNED: apt-get, uv sync, git clone in setup (CLAUDE.md Rule 17).
# T4 BANNED: Turing architecture, no BF16 (CLAUDE.md).
#
# Mirror of train_factorial.yaml infrastructure with HPO-specific overrides.
# UpCloud sunset 2026-03-16 — all references removed. GCS is the only data backend.

name: minivess-hpo

resources:
  # GAR image — Docker registry in europe-west4.
  image_id: docker:europe-west4-docker.pkg.dev/minivess-mlops/minivess/base:latest
  cloud: gcp
  use_spot: true
  disk_size: 100
  disk_tier: best
  accelerators: L4:1
  job_recovery:
    max_restarts_on_errors: 3

# Checkpoint persistence via GCS bucket mount (#878).
file_mounts:
  /app/checkpoints:
    source: gs://minivess-mlops-checkpoints
    mode: MOUNT_CACHED

envs:
  MODEL_FAMILY: dynunet
  HPO_CONFIG: configs/hpo/dynunet_example.yaml

  # MLflow: Cloud Run (GCS artifact store, no basic auth)
  MLFLOW_TRACKING_URI: ${MLFLOW_TRACKING_URI}
  MLFLOW_HTTP_REQUEST_MAX_RETRIES: "10"
  MLFLOW_HTTP_REQUEST_TIMEOUT: "300"

  # Suppress git warning (runner image has no git)
  GIT_PYTHON_REFRESH: quiet

  # Cloud VM escape hatches
  MINIVESS_ALLOW_HOST: "1"
  PREFECT_DISABLED: "1"

  # train_flow.py env requirements
  SPLITS_DIR: /app/configs/splits
  CHECKPOINT_DIR: /app/checkpoints
  LOGS_DIR: /app/logs

  # PyTorch CUDA memory optimization
  PYTORCH_ALLOC_CONF: "expandable_segments:True"

  # HuggingFace token
  HF_TOKEN: ${HF_TOKEN}

setup: |
  set -ex

  # All deps are in the Docker image — setup is DATA ONLY.
  cd /app

  # HuggingFace login for gated models
  if [ -n "${HF_TOKEN}" ]; then
    huggingface-cli login --token "${HF_TOKEN}" --add-to-git-credential 2>/dev/null || true
  fi

  # Data: pull from GCS via DVC (GCP production path)
  echo "Initializing DVC for GCS data pull..."
  dvc init --no-scm -f 2>/dev/null || true
  dvc remote add -f gcs gs://minivess-mlops-dvc-data

  if [ -d "data/raw/minivess/imagesTr" ] && [ "$(ls data/raw/minivess/imagesTr 2>/dev/null | wc -l)" -gt 10 ]; then
    echo "Training data already present ($(ls data/raw/minivess/imagesTr | wc -l) files)"
  else
    echo "Pulling training data from GCS via DVC..."
    dvc pull data/raw/minivess -r gcs || {
      echo "FATAL: DVC pull from GCS failed."
      exit 1
    }
  fi

  # Full splits for HPO
  cp configs/splits/3fold_seed42.json configs/splits/splits.json
  mkdir -p /app/checkpoints /app/logs

  # Pre-cache model weights
  case "${MODEL_FAMILY}" in
    sam3_vanilla|sam3_topolora|sam3_hybrid)
      python -c "from huggingface_hub import hf_hub_download; hf_hub_download('facebook/sam3', 'sam3.pt')" 2>/dev/null || true
      ;;
    vesselfm)
      python -c "from huggingface_hub import hf_hub_download; hf_hub_download('bwittmann/vesselFM', 'vesselFM_base.pt')" 2>/dev/null || true
      ;;
  esac

  # Pre-flight checks
  echo "=== Pre-flight Check ==="
  nvidia-smi
  python -c "import torch, sys; print(f'PyTorch: {torch.__version__}, CUDA: {torch.cuda.is_available()}'); sys.exit(1) if not torch.cuda.is_available() else print(f'GPU: {torch.cuda.get_device_name(0)}')"

run: |
  cd /app

  if [ ! -f configs/splits/splits.json ]; then
    echo "FATAL: splits.json missing — setup did not complete."
    exit 1
  fi

  echo "=== MinIVess HPO Sweep ==="
  echo "Model: ${MODEL_FAMILY}"
  echo "HPO config: ${HPO_CONFIG}"
  echo "MLflow: ${MLFLOW_TRACKING_URI}"

  python -m minivess.optimization.hpo_engine \
    --config "${HPO_CONFIG}" \
    --model-family "${MODEL_FAMILY}"
