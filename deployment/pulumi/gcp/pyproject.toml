[project]
name = "minivess-gcp-infra"
version = "0.1.0"
description = "Pulumi IaC for MinIVess MLOps full stack on GCP"
requires-python = ">=3.12"
dependencies = [
    "pulumi>=3.0,<4.0",
    "pulumi-gcp>=8.0,<9.0",
    "pulumi-docker>=4.0,<5.0",
]
